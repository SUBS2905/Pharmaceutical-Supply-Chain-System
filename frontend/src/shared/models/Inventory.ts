import Product from './Product';

export default class Inventory {
  location: string;
  quantity: number;
  level: string;
  batchNumber: string;
  serialNumber: string;
  productDetails: Product;
}
